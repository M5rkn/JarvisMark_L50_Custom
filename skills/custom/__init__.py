# -*- coding: utf-8 -*-
"""User-created skills live here (one directory per skill, each with a
``manifest.json`` + ``skill.py``). Created at runtime by the SkillManager or
added manually. The 'custom' namespace is intentionally empty by default —
see ``skills/custom/spotify/`` for a complete reference skill."""
